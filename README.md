# 🌐 STM32 Distributed LED Control System (via Ethernet & UART)

Данный проект представляет собой распределенную систему управления световыми эффектами. Система состоит из трех микроконтроллеров STM32 и ПК-приложения, взаимодействующих через каскад интерфейсов: UART -> Ethernet (W5500) -> TCP/UDP.

## 🏗 Архитектура системы

Система разделена на три логических узла и управляющее ПО:

### 1. Узел [Encoder] (Папка: `encoder`)

- **Роль:** Физический интерфейс ввода.
- **Аппаратное обеспечение:** STM32 + Инкрементальный энкодер + Кнопка.
- **Функционал:**
  - Считывание вращения энкодера через аппаратный таймер.
  - Отправка данных по UART в формате `$VAL:XXX\n`.
  - Переключение режимов (Яркость/Цвет) по нажатию кнопки ($BTN).
  - **Синхронизация:** Принимает команды от Мастера и корректирует свой счетчик таймера (`__HAL_TIM_SET_COUNTER`), чтобы физическая "ручка" всегда была синхронизирована с программным значением.

### 2. Узел [Master] (Папка: `Encoder_Master`)

- **Роль:** Центральный шлюз (Gateway).
- **Связи:** UART (к Encoder) <---> Ethernet.
- **Функционал:**
  - Трансляция данных из UART в Ethernet-сеть.
  - Управление модулем **Wiznet W5500**.

### 3. Узел [Blink_Test] (Папка: `Blink_Test`)

- **Роль:** Исполнительное устройство.
- **Аппаратное обеспечение:** STM32 + W5500 + Светодиодная лента WS2812B.
- **Функционал:**
  - Прием команд по Ethernet (TCP/UDP).
  - Управление лентой через DMA + PWM.
  - Плавная динамическая анимация на основе полученных параметров `global_brightness` и `global_hue`.
  - Работа в режиме TCP-сервера для связи с ПК-приложением.
  - Рассылка UDP-статуса другим контроллерам в сети.

### 4. ПО [Desktop App] (Папка: `WinFormsApp1`)

- **Роль:** Удаленный мониторинг и управление.
- **Язык:** C# (WinForms).
- **Функционал:**
  - Визуализация текущих параметров (TrackBar для яркости, ColorWheel для цвета).
  - Прямое управление через TCP-сокеты.
  - Отображение логов обмена данными.

---

## 📡 Схема потоков данных

1.  **Вращение ручки:** `[Encoder]` --(UART)--> `[Master]` --(TCP)--> `[Blink_Test]`--(TCP/UDP)-->`[ПК]`
2.  **Управление с ПК:** `[ПК]` --(TCP)--> `[Blink_Test]` --(UDP)--> `[Master]` --(UART)--> `[Encoder]` (для синхронизации ручки)

---
